the idea is simple.

> infinite canvas design app
> single input -> output from different models.
> different outputs as input.

we will use tldraw sdk. we want to get to first iteration as quickly as we can.
